#include <stdio.h>
/**
 *main - Entry point of program
 *Description: 'prints all the numbers of base 16 in lowercase'
 *Return: always zero
 */
int main(void)
{
	int q;
	int s;

	for (q = 48; q <= 57; q++)
	{
		putchar(q);
	}
	for (s = 97; s <= 102; s++)
	}

