#include "main.h"
/**
 *_strlen - returns the length of a string
 *
 *@s: s is a character
 *
 *Return: value is m
 */

int _strlen(char *s)
{
	int m;

	for (m = 0; s[m] != '\0'; m++)
	{
	}
	return (m);
}

